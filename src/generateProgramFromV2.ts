import { OpenAPIV2 } from "openapi-types";
import { ProgramDefinition } from "./common";

export default function generateProgramFromV2(
  spec: OpenAPIV2.Document
): ProgramDefinition {
  console.log("TODO: OpenAPIV2");
  process.exit(1);
}
